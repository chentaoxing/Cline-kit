# Normalise line endings so a clone on Windows does not rewrite every file on the first commit.
* text=auto eol=lf
*.png binary
*.ico binary
*.zip binary
*.cmd text eol=crlf
*.ps1 text eol=crlf
*.vbs text eol=crlf
